// Single place to point the whole site at your backend.
// Change this one line when you deploy (e.g. to your Render URL).
window.API_BASE_URL = window.API_BASE_URL || "https://sixstarbackend.onrender.com/api";
