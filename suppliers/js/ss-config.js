/* ============================================================
   SIX STAR SUPPLIERS — global config
   Loaded before ss-api.js on every page that uses SS_API.
   ============================================================ */

const SS_CONFIG = {
  // Point this at your deployed backend - no trailing slash
  API_BASE: "https://sixstarbackend.onrender.com/api",
};
